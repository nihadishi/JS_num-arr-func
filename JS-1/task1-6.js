let clientName = prompt("Enter name: ");
let clientSpentToday = prompt("Spent Today:");
if(isNaN(clientSpentToday)){
    console.log("it's not a number. Reload page and try again");
}

let clientSpentAll = prompt("spent All: ");
if(isNaN(clientSpentToday)){
    console.log("it's not a number. Reload page and try again");
}



